# Minimal test for DSL + Shell

To TestVerb a Text
    Print "Verb called with: " followed by "${text}".

Using TestVerb parse {
    hello world
    from dsl
}

Execute the shell command `echo SH_TEST`.

A Text named x exists.
x is `echo captured`.

Print "X = " followed by x.
